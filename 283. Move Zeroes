class Solution {
public:
    void moveZeroes(vector<int>& nums) {
        int num = nums.size();
        int start = 0;

        for(int i = 0; i<num;i++){
            if(nums[i]!=0){
                swap(nums[start++],nums[i]);
            }

        }
    }
};
