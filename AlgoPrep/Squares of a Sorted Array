class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {

      vector<int> check ;
      for(int i = 0; i<nums.size();i++){
         check.push_back(nums[i]*nums[i]);
      }
// square sorted array

        sort(check.begin(), check.end());
        return check;



    }
};
